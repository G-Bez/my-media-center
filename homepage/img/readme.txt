Copy a .jpg image inside this folder and name it "background" in order to set it as homepage backround.

If the image has a different file extension, just modify the ./homepage/config/settings.yaml file accordingly